# Generate the build system
```bash
cmake -B build
```

# Build For Debug
```bash
cmake --build build --config Debug
```

# Build For Release
```bash
cmake --build build --config Release
```

# Run the Release target
```bash
.\build\Debug\F8Cuda.exe
```